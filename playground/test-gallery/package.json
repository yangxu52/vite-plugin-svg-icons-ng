{
  "name": "playground-test-gallery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test:gzip": "npm run build && http-server dist --cors --gzip -c-1",
    "test:br": "npm run build && http-server dist --cors --brotli -c-1",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "vue": "catalog:"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:",
    "http-server": "^14.1.1",
    "vite": "catalog:",
    "vite-plugin-svg-icons-ng": "workspace:*"
  }
}
