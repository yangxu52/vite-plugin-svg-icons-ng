{
  "name": "playground-vite-ssr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build --outDir dist/client --emptyOutDir",
    "build:ssr": "vite build --ssr src/entry-server.ts --outDir dist/server --emptyOutDir false",
    "preview": "node server.js --prod",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plugin-svg-icons-ng": "workspace:*"
  }
}
